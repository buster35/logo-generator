const fs = require("fs")
const writeToSVG = require("./index.js")
const path = require("path")
const fileName = fs.writeFile(`./examples/${response.logoCharacters}.SVG`)


function generateLogo(response) {






}